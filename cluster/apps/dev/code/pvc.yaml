---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-server-data-v2
  namespace: dev
spec:
  storageClassName: nfs-client
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
